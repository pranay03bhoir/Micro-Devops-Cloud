spring:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 60000
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: false
              lower-case-service-id: true
  application:
    name: API-GATEWAY

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    gateway:
      access: READ_ONLY

server:
  port: 8080

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}

resilience4j.circuitbreaker:
  configs:
    default:
      sliding-window-size: 10
      permitted-number-of-calls-in-half-open-state: 2
      failure-rate-threshold: 50
      wait-duration-in-open-state: 10000
logging:
  level:
    root: INFO
  file:
    name: logs/${spring.application.name}.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] traceId=%X{trace_id} spanId=%X{span_id} %logger{36} : %msg%n"
    console: "%d{HH:mm:ss} %-5level traceId=%X{trace_id} spanId=%X{span_id} %logger{36} : %msg%n"
PRODUCT_SERVICE_ID: lb://PRODUCTS-SERVICE
INVENTORY_SERVICE_ID: lb://INVENTORY-SERVICE
CART_ORDER_SERVICE_ID: lb://CART-ORDER-SERVICE
USER_SERVICE_ID: lb://USER-SERVICE

jwt:
  secret: '${JWT_SECRET:DEv(Nk8U}x(Z#%@*cfdN)IgY$2M/FeiptC}qk7_[>i1cW(Ax&0bI@k!{{;2]?qt>YQ^BOChy^$h<WM^yF]y0Hn}'
