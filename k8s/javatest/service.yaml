apiVersion: v1
kind: Service
metadata:
  name: javatest-service

spec:
  selector:
    app: javatest

  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8080
    # ClusterIP is used to access within the cluster.
  #type: ClusterIP
  type: LoadBalancer
