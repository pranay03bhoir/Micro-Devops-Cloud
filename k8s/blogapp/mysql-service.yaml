apiVersion: v1
kind: Service
metadata:
  name: mysql-service

spec:
  selector:
    app: mysql

  ports:
    - protocol: TCP
      port: 3308
      targetPort: 3306
    # ClusterIP is used to access within the cluster.
  #type: ClusterIP
  type: ClusterIP
