spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: ${GITHUB_URI:https://github.com/pranay03bhoir/easy_buy_config.git}
          username: ${GITHUB_USERNAME:pranay03bhoir}
          password: ${GITHUB_PASSWORD}
          defaultLabel: main

server:
  port: 8079

encrypt:
   key: 7c5fe7269de475aae03682fd238767b89a34798053e45969bbf08b6ddb801a3

management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka}
